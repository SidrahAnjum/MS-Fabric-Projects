# Screenshots

Place captured screenshots here, for example:

- pl_master_orchestrator canvas (full view, and Switch_SourceType detail)
- pl_fills_incremental canvas
- Semantic model relationships (Model view)
- Eventstream canvas (pos_producer -> es_pos_adjudication -> destination)
- The Reflex/Activator rule (rule_fills_file_arrival)
- Dynamic Data Masking verification (sys.masked_columns query result)
- Monitoring hub run history showing a successful end-to-end run
