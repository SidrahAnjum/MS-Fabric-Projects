# Pipeline definitions

Fabric's Git integration was not available on this account, so pipeline
definitions could not be synced automatically. If pipeline JSON is ever
exported (via the Fabric REST API or the fabric-cicd tool), place it here.

In the meantime, the full pipeline design, every activity, and every
expression used are documented in:

- /docs/PROJECT_STATUS.md
- /docs/fabric-pharmacy-project-complete-guide.docx
- /docs/screenshots/ (visual reference)
